# Streaming security

Add rolling output inspection with a configurable holdback buffer so sensitive or unsafe content is not necessarily released before a decision can be made.

Measure bytes/tokens leaked before termination, time-to-block, and false interruption rate. Tool calls must always be fully validated before execution even when visible text is streamed.
