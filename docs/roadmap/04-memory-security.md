# Memory security

Treat persistent memory writes as security-sensitive events.

Memory records should preserve provenance and authority. Untrusted external information may be stored as data but must not silently establish authorization, credentials, tool permissions, application policy, or user approval.
