# Model output

Use `inspect_model_output()` for output-side detectors such as secret leakage.

The current default policy is intentionally simple and may not match every application's desired behavior. Future policies will support redaction/sanitization rather than only allow/block behavior.
