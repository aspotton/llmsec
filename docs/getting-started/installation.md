# Installation

During development:

```bash
python -m pip install -e .
```

Development tools:

```bash
python -m pip install -e '.[dev]'
```

The base V0.1 runtime has no third-party dependencies. Optional model runtimes will remain extras rather than implicit dependencies.
